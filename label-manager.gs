function runLabelManager() {
  // examples
  manageLabel('ThisWeek', 'in:inbox newer_than:14d');
  manageLabel('Today', 'in:inbox newer_than:1d');
  manageLabel('DEAL WITH ME', 'in:inbox older_than:14d');
}
